import { lazy } from 'react';

export const emptyList = lazy(() => import('./emptyList.svg'));
export const error = lazy(() => import('./error.svg'));
export const networkError = lazy(() => import('./networkError.svg'));
